# Copy to .env and adjust if needed. Defaults match docker-compose.yml.
DATABASE_URL=postgresql+psycopg2://anupalan:anupalan@localhost:5432/anupalan

# JWT — demo values, fine for the hackathon
JWT_SECRET=anupalan-dev-secret-change-me
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=720

# Evidence photos land here
STORAGE_DIR=./storage

# Dashboard origin, for CORS
CORS_ORIGINS=http://localhost:5173
